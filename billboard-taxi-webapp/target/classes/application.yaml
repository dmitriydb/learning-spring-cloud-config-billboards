server:
  port: 9001
spring:
  application:
    name: billboard-taxi
  profiles:
    active: production
  config:
    import: optional:configserver:http://localhost:8888
management:
  endpoints:
    web:
      exposure:
        include: '*'