spring:
  application:
    name: billboard-ads
  cloud:
    config:
      username: root
      name: billboard-ads
      password: c3rb3rus
      fail-fast: true
      retry:
        max-attempts: 100